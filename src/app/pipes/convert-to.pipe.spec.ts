import { ConvertToPipe } from './convert-to.pipe';

describe('ConvertToPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToPipe();
    expect(pipe).toBeTruthy();
  });
});
