import { PipesConvertToSpacePipe } from './pipes-convert-to-space.pipe';

describe('PipesConvertToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new PipesConvertToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
